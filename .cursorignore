# === ESSENTIEL POUR ELECTRON ===

# Dépendances (toujours ignorer)
node_modules/
npm-debug.log*

# Environnement et secrets (sécurité)
.env
.env.local
.env.development.local
.env.production.local

# ⚠️ IMPORTANT: dist/ n'est PAS ignoré car nécessaire pour Electron
# Le dossier dist contient les builds main/preload/renderer utilisés par Electron

# Electron build final (packaging)
release/
out/

# Cache temporaires
.cache/
.tmp/
node_modules/.cache/

# OS (masqués)
.DS_Store
Thumbs.db

# IDE personnel (garder la config de base)
.idea/
*.swp
*~
